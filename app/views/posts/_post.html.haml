- if posts.present?
  - posts.each do |post|
    .post-container
      .post-container__top
        .post-container__top__left
          - if post.user.img.present?
            = image_tag(post.user.img.to_s, class: "post-container__top__left__userimg")
          - else
            = image_tag("default_user.png", class: "post-container__top__left__userimg")
          .post-container__top__left__contents__title
            = link_to post.title, post_path(post.id)
            .post-container__top__left__contents__name
              = link_to post.user.nickname, user_path(post.user.id)
        .post-container__top__right
          - if post.post_type == "バンドメンバー募集"
            .post-container__top__right--recruit
              = post.post_type_icon
          - elsif post.post_type == "バンド参加希望"
            .post-container__top__right--entry
              = post.post_type_icon
          - else
            .post-container__top__right--other
              = post.post_type_icon
      .post-container__bottom
        .post-container__bottom__contents
          = post.contents_cut
        .post-container__bottom__date
          投稿日時：
          = post.created_at.strftime("%Y/%m/%d %H:%M")
          %br
          更新日時：
          = post.updated_at.strftime("%Y/%m/%d %H:%M")
- else
  .nothing-default
    投稿した記事はありません